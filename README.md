For OCP :-
get into directory :- cd OCP
compile :- g++ Main.cpp source/*.cpp -Iheader 
run :- a.out

For SRP :- 
get into directory :- cd SRP
compile :- g++ Main.cpp source/*.cpp -Iheader
run :- a.out
